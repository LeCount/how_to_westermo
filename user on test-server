# Log in as admin on tests-server
(1) ssh wadmin@TEST_SERVER

# Add new user
(2) sudo adduser USER

# User is now created but locked, set password for user to unlock it
(3) sudo passwd USER

# Add user to sudo-group
(4) sudo adduser USER sudo
